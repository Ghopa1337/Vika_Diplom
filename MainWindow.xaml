<Window x:Class="CargoTransport.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CargoTransport.Desktop.ViewModels"
        xmlns:behaviors="clr-namespace:CargoTransport.Desktop.Behaviors"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:CargoTransport.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="Система учета грузоперевозок"
        Width="1200"
        Height="760"
        MinWidth="980"
        MinHeight="620"
        Icon="Assets/AppIcon.png"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Style="{StaticResource MaterialDesignWindow}">
    <Window.Resources>
        <Style x:Key="SectionDataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="CanUserSortColumns" Value="False" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#E5ECF1" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC" />
            <Setter Property="HorizontalGridLinesBrush" Value="#EEF2F5" />
            <Setter Property="VerticalGridLinesBrush" Value="#EEF2F5" />
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="FontSize" Value="13" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="Foreground" Value="#51606D" />
                        <Setter Property="Background" Value="#F5F8FB" />
                        <Setter Property="BorderBrush" Value="#F5F8FB" />
                        <Setter Property="Padding" Value="14,12" />
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="14,12" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Foreground" Value="#22303A" />
                        <Setter Property="FontSize" Value="13" />
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CrudTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="Margin" Value="0,0,10,10" />
            <Setter Property="MinWidth" Value="160" />
        </Style>

        <Style x:Key="CrudComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
            <Setter Property="Margin" Value="0,0,10,10" />
            <Setter Property="MinWidth" Value="160" />
        </Style>

        <Style x:Key="CrudButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Margin" Value="0,0,10,10" />
            <Setter Property="MinWidth" Value="100" />
        </Style>

        <Style x:Key="SectionExpanderStyle" TargetType="Expander">
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style>

        <DataTemplate DataType="{x:Type viewModels:OrderWizardViewModel}">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Пошаговое создание заказа"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#22303A" />
                    <TextBlock Margin="0,8,0,0"
                               Text="Сценарий заменяет ручное создание через CRUD-форму: сначала груз, затем маршрут и только потом расчет или назначение."
                               FontSize="13"
                               TextWrapping="Wrap"
                               Foreground="#687683" />

                    <StackPanel Margin="0,20,0,0"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <StackPanel Width="120">
                            <Ellipse Width="34"
                                     Height="34"
                                     HorizontalAlignment="Center"
                                     Fill="{Binding CurrentStep, Converter={local:StepToColorConverter}, ConverterParameter=1}"
                                     Stroke="#0F5C69"
                                     StrokeThickness="2" />
                            <TextBlock Margin="0,10,0,0"
                                       Text="Груз"
                                       HorizontalAlignment="Center"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#50606D" />
                        </StackPanel>

                        <Rectangle Width="56"
                                   Height="2"
                                   VerticalAlignment="Center"
                                   Fill="#C8D6DF" />

                        <StackPanel Width="120">
                            <Ellipse Width="34"
                                     Height="34"
                                     HorizontalAlignment="Center"
                                     Fill="{Binding CurrentStep, Converter={local:StepToColorConverter}, ConverterParameter=2}"
                                     Stroke="#0F5C69"
                                     StrokeThickness="2" />
                            <TextBlock Margin="0,10,0,0"
                                       Text="Маршрут"
                                       HorizontalAlignment="Center"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#50606D" />
                        </StackPanel>

                        <Rectangle Width="56"
                                   Height="2"
                                   VerticalAlignment="Center"
                                   Fill="#C8D6DF" />

                        <StackPanel Width="120">
                            <Ellipse Width="34"
                                     Height="34"
                                     HorizontalAlignment="Center"
                                     Fill="{Binding CurrentStep, Converter={local:StepToColorConverter}, ConverterParameter=3}"
                                     Stroke="#0F5C69"
                                     StrokeThickness="2" />
                            <TextBlock Margin="0,10,0,0"
                                       Text="Назначение"
                                       HorizontalAlignment="Center"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#50606D" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="1"
                        CornerRadius="24"
                        Background="#F8FBFD"
                        BorderBrush="#E6EEF3"
                        BorderThickness="1">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="22">
                            <StackPanel Visibility="{Binding CurrentStep, Converter={local:StepToVisibilityConverter}, ConverterParameter=1}">
                                <TextBlock Text="Шаг 1. Получатель и груз"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="#22303A" />
                                <TextBlock Margin="0,8,0,0"
                                           Text="Выберите получателя и заполните параметры нового груза. При необходимости можно взять уже существующий груз из справочника."
                                           FontSize="13"
                                           TextWrapping="Wrap"
                                           Foreground="#687683" />

                                <ComboBox Margin="0,18,0,0"
                                          SelectedValue="{Binding SelectedReceiverUserId}"
                                          IsEnabled="{Binding IsReceiverSelectionEnabled}"
                                          SelectedValuePath="Id"
                                          DisplayMemberPath="DisplayName"
                                          ItemsSource="{Binding ReceiverOptions}"
                                          materialDesign:HintAssist.Hint="Получатель" />

                                <CheckBox Margin="0,18,0,0"
                                          Content="Создать новый груз"
                                          IsChecked="{Binding IsNewCargo}" />

                                <StackPanel Margin="0,18,0,0"
                                            Visibility="{Binding IsNewCargo, Converter={local:BooleanToVisibilityConverter}}">
                                    <WrapPanel>
                                        <TextBox Margin="0,0,10,10"
                                                 Text="{Binding CargoName, UpdateSourceTrigger=PropertyChanged}"
                                                 materialDesign:HintAssist.Hint="Наименование груза" />
                                        <ComboBox Margin="0,0,10,10"
                                                  SelectedValue="{Binding SelectedCargoType}"
                                                  SelectedValuePath="Tag"
                                                  materialDesign:HintAssist.Hint="Тип груза">
                                            <ComboBoxItem Content="Обычный" Tag="normal" />
                                            <ComboBoxItem Content="Опасный" Tag="hazardous" />
                                            <ComboBoxItem Content="Скоропортящийся" Tag="perishable" />
                                            <ComboBoxItem Content="Крупногабаритный" Tag="oversized" />
                                        </ComboBox>
                                        <TextBox Margin="0,0,10,10"
                                                 Text="{Binding CargoWeight, UpdateSourceTrigger=PropertyChanged}"
                                                 materialDesign:HintAssist.Hint="Вес, кг" />
                                        <TextBox Margin="0,0,10,10"
                                                 Text="{Binding CargoVolume, UpdateSourceTrigger=PropertyChanged}"
                                                 materialDesign:HintAssist.Hint="Объем, м3" />
                                    </WrapPanel>

                                    <TextBox Height="84"
                                             Text="{Binding SpecialRequirements, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True"
                                             TextWrapping="Wrap"
                                             materialDesign:HintAssist.Hint="Особые требования к перевозке" />
                                </StackPanel>

                                <StackPanel Margin="0,18,0,0"
                                            Visibility="{Binding IsNewCargo, Converter={local:InverseBooleanToVisibilityConverter}}">
                                    <ComboBox SelectedValue="{Binding SelectedExistingCargoId}"
                                              SelectedValuePath="Id"
                                              DisplayMemberPath="DisplayName"
                                              ItemsSource="{Binding CargoOptions}"
                                              materialDesign:HintAssist.Hint="Груз из справочника" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding CurrentStep, Converter={local:StepToVisibilityConverter}, ConverterParameter=2}">
                                <TextBlock Text="Шаг 2. Маршрут и телефоны"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="#22303A" />
                                <TextBlock Margin="0,8,0,0"
                                           Text="Заполните точки маршрута, укажите телефоны водителя и получателя и при необходимости задайте плановые даты."
                                           FontSize="13"
                                           TextWrapping="Wrap"
                                           Foreground="#687683" />

                                <WrapPanel Margin="0,18,0,0">
                                    <TextBox Margin="0,0,10,10"
                                             Text="{Binding PickupAddress, UpdateSourceTrigger=PropertyChanged}"
                                             materialDesign:HintAssist.Hint="Адрес погрузки" />
                                    <TextBox Margin="0,0,10,10"
                                             Text="{Binding DeliveryAddress, UpdateSourceTrigger=PropertyChanged}"
                                             materialDesign:HintAssist.Hint="Адрес доставки" />
                                    <TextBox Margin="0,0,10,10"
                                             Text="{Binding PickupContactPhone, UpdateSourceTrigger=PropertyChanged}"
                                             materialDesign:HintAssist.Hint="Телефон водителя" />
                                    <TextBox Margin="0,0,10,10"
                                             Text="{Binding DeliveryContactPhone, UpdateSourceTrigger=PropertyChanged}"
                                             materialDesign:HintAssist.Hint="Телефон получателя" />
                                    <DatePicker Margin="0,0,10,10"
                                                MinWidth="160"
                                                SelectedDate="{Binding PlannedPickupAt}"
                                                DisplayDateStart="{Binding MinSelectableDate, Mode=OneWay}"
                                                materialDesign:HintAssist.Hint="План погрузки" />
                                    <DatePicker Margin="0,0,10,10"
                                                MinWidth="160"
                                                SelectedDate="{Binding DesiredDeliveryAt}"
                                                DisplayDateStart="{Binding MinSelectableDate, Mode=OneWay}"
                                                materialDesign:HintAssist.Hint="Желаемая доставка" />
                                </WrapPanel>

                                <TextBox Height="84"
                                         Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         materialDesign:HintAssist.Hint="Комментарий к заказу" />
                            </StackPanel>

                            <StackPanel Visibility="{Binding CurrentStep, Converter={local:StepToVisibilityConverter}, ConverterParameter=3}">
                                <TextBlock Text="Шаг 3. Расчет и назначение"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="#22303A" />
                                <TextBlock Margin="0,8,0,0"
                                           Text="Стоимость рассчитывается автоматически по расстоянию. При необходимости сразу закрепите экипаж. Если выбрать и водителя, и транспорт, заказ будет создан со статусом «Назначен»."
                                           FontSize="13"
                                           TextWrapping="Wrap"
                                           Foreground="#687683" />

                                <WrapPanel Margin="0,18,0,0">
                                    <TextBox Margin="0,0,10,10"
                                             Text="{Binding DistanceKm, UpdateSourceTrigger=PropertyChanged}"
                                             materialDesign:HintAssist.Hint="Расстояние, км" />
                                    <TextBox Margin="0,0,10,10"
                                             Text="{Binding TotalCost, UpdateSourceTrigger=PropertyChanged}"
                                             IsReadOnly="True"
                                             materialDesign:HintAssist.Hint="Стоимость перевозки" />
                                    <ComboBox Margin="0,0,10,10"
                                              SelectedValue="{Binding SelectedDriverId}"
                                              SelectedValuePath="Id"
                                              DisplayMemberPath="DisplayName"
                                              ItemsSource="{Binding DriverOptions}"
                                              Visibility="{Binding IsAssignmentVisible, Converter={local:BooleanToVisibilityConverter}}"
                                              materialDesign:HintAssist.Hint="Водитель" />
                                    <ComboBox Margin="0,0,10,10"
                                              SelectedValue="{Binding SelectedVehicleId}"
                                              SelectedValuePath="Id"
                                              DisplayMemberPath="DisplayName"
                                              ItemsSource="{Binding VehicleOptions}"
                                              Visibility="{Binding IsAssignmentVisible, Converter={local:BooleanToVisibilityConverter}}"
                                              materialDesign:HintAssist.Hint="Транспорт" />
                                </WrapPanel>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Border>

                <Grid Grid.Row="2" Margin="0,18,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding StatusMessage}"
                               TextWrapping="Wrap"
                               Foreground="#0F5C69" />

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Width="110"
                                Margin="0,0,10,0"
                                Command="{Binding BackCommand}"
                                Content="Назад" />
                        <Button Width="110"
                                Margin="0,0,10,0"
                                Command="{Binding NextCommand}"
                                Content="Далее"
                                Visibility="{Binding CurrentStep, Converter={local:StepToVisibilityConverter}, ConverterParameter=NotLast}" />
                        <Button Width="160"
                                Background="#D8F3E3"
                                Command="{Binding CompleteCommand}"
                                Content="Создать заказ"
                                Visibility="{Binding CurrentStep, Converter={local:StepToVisibilityConverter}, ConverterParameter=3}" />
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:OrderRequestWizardViewModel}">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Новая заявка"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#22303A" />
                    <TextBlock Margin="0,10,0,0"
                               Text="Получатель заполняет только основные данные: дата, маршрут, груз и контакты. Диспетчер потом оформит полноценный заказ."
                               Foreground="#687683"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Row="1"
                        Padding="22"
                        CornerRadius="24"
                        Background="White"
                        BorderBrush="#E8EEF2"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="#0F5C69"
                                   TextWrapping="Wrap" />

                        <WrapPanel Margin="0,18,0,0">
                            <DatePicker Margin="0,0,10,10"
                                        MinWidth="160"
                                        materialDesign:HintAssist.Hint="Желаемая дата"
                                        SelectedDate="{Binding DesiredDate, Mode=TwoWay}"
                                        DisplayDateStart="{Binding MinSelectableDate, Mode=OneWay}" />
                            <TextBox Style="{StaticResource CrudTextBoxStyle}"
                                     materialDesign:HintAssist.Hint="Груз"
                                     Text="{Binding CargoDescription, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Style="{StaticResource CrudTextBoxStyle}"
                                     materialDesign:HintAssist.Hint="Откуда"
                                     Text="{Binding PickupAddress, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Style="{StaticResource CrudTextBoxStyle}"
                                     materialDesign:HintAssist.Hint="Куда"
                                     Text="{Binding DeliveryAddress, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Style="{StaticResource CrudTextBoxStyle}"
                                     materialDesign:HintAssist.Hint="Телефон на погрузке"
                                     Text="{Binding PickupContactPhone, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Style="{StaticResource CrudTextBoxStyle}"
                                     materialDesign:HintAssist.Hint="Телефон получателя"
                                     Text="{Binding DeliveryContactPhone, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Grid.ColumnSpan="2"
                                     Style="{StaticResource CrudTextBoxStyle}"
                                     materialDesign:HintAssist.Hint="Комментарий"
                                     Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="2" Margin="0,20,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Width="180"
                            Background="#D8F3E3"
                            Command="{Binding SubmitCommand}"
                            Content="Отправить заявку" />
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:OrderDetailsViewModel}">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding OrderNumber}"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#22303A" />
                        <TextBlock Margin="0,8,24,0"
                                   Text="{Binding StatusHint}"
                                   FontSize="13"
                                   TextWrapping="Wrap"
                                   Foreground="#6A7784" />
                    </StackPanel>

                    <Border Grid.Column="1"
                            Padding="16,12"
                            CornerRadius="18"
                            Background="#E8F5F7"
                            VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="Статус заказа"
                                       FontSize="11"
                                       Foreground="#4F7A81" />
                            <TextBlock Margin="0,4,0,0"
                                       Text="{Binding StatusLabel}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       Foreground="#0F5C69" />
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="1"
                        Margin="0,22,0,22"
                        Padding="18"
                        CornerRadius="24"
                        Background="#F8FBFD"
                        BorderBrush="#E6EEF3"
                        BorderThickness="1">
                    <ItemsControl ItemsSource="{Binding StatusSteps}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="1" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,8,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Border Width="34"
                                            Height="34"
                                            Background="{Binding BadgeBackground}"
                                            BorderBrush="{Binding BadgeBorderBrush}"
                                            BorderThickness="2"
                                            CornerRadius="17">
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Text="●"
                                                   Foreground="{Binding BadgeForeground}" />
                                    </Border>

                                    <StackPanel Grid.Column="1"
                                                Margin="12,0,0,0"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{Binding TitleForeground}" />
                                        <TextBlock Margin="0,4,0,0"
                                                   Text="{Binding Caption}"
                                                   FontSize="11"
                                                   Foreground="#7B8894" />
                                        <Border Margin="0,8,0,0"
                                                Height="3"
                                                Background="{Binding ConnectorBrush}"
                                                CornerRadius="2" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.45*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <ScrollViewer Margin="0,0,18,0" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <WrapPanel>
                                <Border Margin="0,0,12,12" Padding="16" CornerRadius="18" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Стоимость" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,8,0,0" Text="{Binding CostLabel}" FontSize="18" FontWeight="SemiBold" Foreground="#22303A" />
                                    </StackPanel>
                                </Border>
                                <Border Margin="0,0,12,12" Padding="16" CornerRadius="18" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Расстояние" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,8,0,0" Text="{Binding DistanceLabel}" FontSize="18" FontWeight="SemiBold" Foreground="#22303A" />
                                    </StackPanel>
                                </Border>
                                <Border Margin="0,0,12,12" Padding="16" CornerRadius="18" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Создан" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,8,0,0" Text="{Binding CreatedAtLabel}" FontSize="16" FontWeight="SemiBold" Foreground="#22303A" />
                                    </StackPanel>
                                </Border>
                                <Border Margin="0,0,12,12" Padding="16" CornerRadius="18" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Обновлён" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,8,0,0" Text="{Binding UpdatedAtLabel}" FontSize="16" FontWeight="SemiBold" Foreground="#22303A" />
                                    </StackPanel>
                                </Border>
                            </WrapPanel>

                            <WrapPanel>
                                <Border Margin="0,0,12,12" Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Участники и груз" FontSize="17" FontWeight="SemiBold" Foreground="#22303A" />
                                        <TextBlock Margin="0,14,0,0" Text="Получатель" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,4,0,0" Text="{Binding ReceiverLabel}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                        <TextBlock Margin="0,12,0,0" Text="Водитель" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,4,0,0" Text="{Binding DriverLabel}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                        <TextBlock Margin="0,12,0,0" Text="Транспорт" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,4,0,0" Text="{Binding VehicleLabel}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                        <Separator Margin="0,14,0,14" />
                                        <TextBlock Text="Груз" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,4,0,0" Text="{Binding CargoName}" FontSize="15" FontWeight="SemiBold" Foreground="#22303A" />
                                        <TextBlock Margin="0,8,0,0" Text="{Binding CargoTypeLabel}" FontSize="13" Foreground="#0F5C69" />
                                        <TextBlock Margin="0,6,0,0" Text="{Binding CargoMetrics}" FontSize="13" Foreground="#5F6D79" />
                                        <TextBlock Margin="0,10,0,0" Text="{Binding CargoRequirements}" TextWrapping="Wrap" FontSize="13" Foreground="#5F6D79" />
                                    </StackPanel>
                                </Border>

                                <Border Margin="0,0,12,12" Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Маршрут и сроки" FontSize="17" FontWeight="SemiBold" Foreground="#22303A" />
                                        <TextBlock Margin="0,14,0,0" Text="{Binding PickupLabel}" TextWrapping="Wrap" LineHeight="21" FontSize="13" Foreground="#22303A" />
                                        <Separator Margin="0,14,0,14" />
                                        <TextBlock Text="{Binding DeliveryLabel}" TextWrapping="Wrap" LineHeight="21" FontSize="13" Foreground="#22303A" />
                                        <Separator Margin="0,14,0,14" />
                                        <TextBlock Text="Планирование" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,6,0,0" Text="{Binding ScheduleLabel}" TextWrapping="Wrap" LineHeight="21" FontSize="13" Foreground="#22303A" />
                                        <TextBlock Margin="0,14,0,0" Text="Комментарий" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,6,0,0" Text="{Binding CommentLabel}" TextWrapping="Wrap" LineHeight="21" FontSize="13" Foreground="#22303A" />
                                        <TextBlock Margin="0,14,0,0" Text="Причина отмены" FontSize="12" Foreground="#6A7784" />
                                        <TextBlock Margin="0,6,0,0" Text="{Binding CancellationLabel}" TextWrapping="Wrap" LineHeight="21" FontSize="13" Foreground="#22303A" />
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Column="1">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Действия" FontSize="17" FontWeight="SemiBold" Foreground="#22303A" />
                                <TextBlock Margin="0,10,0,0" Text="{Binding StatusHint}" TextWrapping="Wrap" FontSize="13" Foreground="#687683" />
                                <Button Margin="0,16,0,0" Height="42" Content="Принять заказ" Command="{Binding UpdateStatusCommand}" Visibility="{Binding CanDriverAccept, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Margin="0,12,0,0" Height="42" Content="Начать погрузку" Command="{Binding UpdateStatusCommand}" Visibility="{Binding CanDriverLoad, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Margin="0,12,0,0" Height="42" Content="Отправить в путь" Command="{Binding UpdateStatusCommand}" Visibility="{Binding CanDriverDispatch, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Margin="0,12,0,0" Height="42" Content="Отметить доставку" Command="{Binding UpdateStatusCommand}" Visibility="{Binding CanDriverDeliver, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Margin="0,12,0,0" Height="42" Content="Подтвердить получение" Command="{Binding UpdateStatusCommand}" Visibility="{Binding CanReceiverConfirm, Converter={local:BooleanToVisibilityConverter}}" />
                            </StackPanel>
                        </Border>

                        <Border Margin="0,18,0,0" Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="История изменений" FontSize="17" FontWeight="SemiBold" Foreground="#22303A" />
                                <TextBlock Margin="0,8,0,0" Text="{Binding HistorySummary}" FontSize="12" Foreground="#6A7784" />
                                <ScrollViewer Margin="0,16,0,0" Height="320" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding Timeline}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,16">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>

                                                    <Border Width="12"
                                                            Height="12"
                                                            Margin="0,6,12,0"
                                                            Background="{Binding DotBackground}"
                                                            CornerRadius="6"
                                                            VerticalAlignment="Top" />

                                                    <Border Grid.Column="1"
                                                            Padding="14"
                                                            CornerRadius="16"
                                                            Background="#F8FBFD"
                                                            BorderBrush="#E8EEF2"
                                                            BorderThickness="1">
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                                            <TextBlock Margin="0,6,0,0" Text="{Binding Subtitle}" TextWrapping="Wrap" FontSize="12" Foreground="#5F6D79" />
                                                            <TextBlock Margin="0,10,0,0" Text="{Binding Actor}" FontSize="12" Foreground="#0F5C69" />
                                                            <TextBlock Margin="0,4,0,0" Text="{Binding Time}" FontSize="11" Foreground="#8A97A3" />
                                                        </StackPanel>
                                                    </Border>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="3"
                            Margin="0,22,0,0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Width="110"
                            Content="Закрыть"
                            Command="{Binding CloseCommand}" />
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:UserProfileSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,18,0" Padding="26" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Foreground="#22303A" />
                            <TextBlock Margin="0,12,0,0" Text="{Binding Subtitle}" FontSize="14" LineHeight="22" TextWrapping="Wrap" Foreground="#687683" />
                            <WrapPanel Margin="0,18,0,0">
                                <Button Margin="0,0,10,0" Command="{Binding RefreshCommand}" Content="Обновить" Style="{StaticResource MaterialDesignOutlinedButton}" />
                                <TextBlock Margin="8,8,0,0" Text="{Binding StatusMessage}" Foreground="#0F5C69" TextWrapping="Wrap" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Padding="24" CornerRadius="24" Background="#F5FBFB" BorderBrush="#D5ECEE" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Учетная запись" FontSize="14" FontWeight="SemiBold" Foreground="#0F5C69" />
                            <TextBlock Margin="0,10,0,0" Text="{Binding Username}" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                            <TextBlock Margin="0,6,0,0" Text="{Binding RoleName}" FontSize="13" Foreground="#4E7B81" />
                            <TextBlock Margin="0,14,0,0" Text="{Binding AccountStatus}" FontSize="13" FontWeight="SemiBold" Foreground="#0F5C69" />
                            <Border Margin="0,14,0,0" Padding="12" CornerRadius="14" Background="#FFF4E6" Visibility="{Binding MustChangePassword, Converter={local:BooleanToVisibilityConverter}}">
                                <TextBlock Text="Для учетной записи включено требование сменить пароль." TextWrapping="Wrap" Foreground="#815B19" />
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Padding="24" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Контактные данные" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                                <TextBlock Margin="0,8,0,18" Text="Эти данные видит диспетчер при работе с заказом и рейсом." Foreground="#687683" />

                                <WrapPanel>
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="ФИО / контактное лицо" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Компания" Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Email *" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Телефон *" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />
                                </WrapPanel>

                                <Button Margin="0,8,0,0" Width="170" HorizontalAlignment="Left" Command="{Binding SaveProfileCommand}" Content="Сохранить профиль" Style="{StaticResource MaterialDesignRaisedButton}" />
                            </StackPanel>
                        </Border>

                        <Border Margin="0,18,0,0" Padding="24" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Смена пароля" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                                <TextBlock Margin="0,8,0,18" Text="Отдельный безопасный сценарий: текущий пароль проверяется, старый хэш сохраняется в истории." Foreground="#687683" TextWrapping="Wrap" />

                                <WrapPanel>
                                    <PasswordBox Margin="0,0,10,10"
                                                 MinWidth="160"
                                                 behaviors:PasswordBoxAssistant.BindPassword="True"
                                                 behaviors:PasswordBoxAssistant.BoundPassword="{Binding CurrentPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 materialDesign:HintAssist.Hint="Текущий пароль" />
                                    <PasswordBox Margin="0,0,10,10"
                                                 MinWidth="160"
                                                 behaviors:PasswordBoxAssistant.BindPassword="True"
                                                 behaviors:PasswordBoxAssistant.BoundPassword="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 materialDesign:HintAssist.Hint="Новый пароль" />
                                    <PasswordBox Margin="0,0,10,10"
                                                 MinWidth="160"
                                                 behaviors:PasswordBoxAssistant.BindPassword="True"
                                                 behaviors:PasswordBoxAssistant.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 materialDesign:HintAssist.Hint="Повтор пароля" />
                                </WrapPanel>

                                <WrapPanel Margin="0,8,0,0">
                                    <Button Margin="0,0,10,0" Width="170" Command="{Binding ChangePasswordCommand}" Content="Сменить пароль" Style="{StaticResource MaterialDesignRaisedButton}" />
                                    <Button Command="{Binding ClearPasswordCommand}" Content="Очистить поля" Style="{StaticResource MaterialDesignOutlinedButton}" />
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Margin="0,0,18,0" Padding="20" CornerRadius="22" Background="#F7FAFC" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Активность" FontSize="16" FontWeight="SemiBold" Foreground="#22303A" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding LastLoginLabel, StringFormat=Последний вход: {0}}" Foreground="#687683" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding CreatedAtLabel, StringFormat=Создан: {0}}" Foreground="#687683" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding UpdatedAtLabel, StringFormat=Обновлен: {0}}" Foreground="#687683" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Padding="20" CornerRadius="22" Background="#F7FAFC" BorderBrush="#E8EEF2" BorderThickness="1" Visibility="{Binding IsDriverProfileVisible, Converter={local:BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="Данные водителя" FontSize="16" FontWeight="SemiBold" Foreground="#22303A" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding DriverLicenseLabel, StringFormat=ВУ: {0}}" Foreground="#687683" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding DriverStatusLabel, StringFormat=Статус: {0}}" Foreground="#687683" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:UserNotificationsSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Margin="0,0,0,18" Padding="26" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Foreground="#22303A" />
                            <TextBlock Margin="0,10,0,0" Text="{Binding Subtitle}" FontSize="14" LineHeight="22" TextWrapping="Wrap" Foreground="#687683" />
                            <TextBlock Margin="0,12,0,0" Text="{Binding StatusMessage}" Foreground="#0F5C69" TextWrapping="Wrap" />
                        </StackPanel>
                        <WrapPanel Grid.Column="1" Margin="18,0,0,0" VerticalAlignment="Center">
                            <CheckBox Margin="0,0,12,0" VerticalAlignment="Center" Content="Только непрочитанные" IsChecked="{Binding UnreadOnly, Mode=TwoWay}" />
                            <Button Margin="0,0,10,0" Command="{Binding ApplyFilterCommand}" Content="Фильтр" Style="{StaticResource MaterialDesignOutlinedButton}" />
                            <Button Command="{Binding ClearFilterCommand}" Content="Очистить" Style="{StaticResource MaterialDesignOutlinedButton}" />
                        </WrapPanel>
                    </Grid>
                </Border>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Metrics}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="18" CornerRadius="18" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Value}" FontSize="22" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding Hint}" TextWrapping="Wrap" FontSize="12" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.6*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,18,0" CornerRadius="22" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <DataGrid ItemsSource="{Binding Notifications}"
                                  SelectedItem="{Binding SelectedNotification, Mode=TwoWay}"
                                  Style="{StaticResource SectionDataGridStyle}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Статус" Binding="{Binding ReadState}" Width="0.8*" />
                                <DataGridTextColumn Header="Тип" Binding="{Binding TypeName}" Width="0.9*" />
                                <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="1.6*" />
                                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAtLabel}" Width="1.1*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Border Grid.Column="1" Padding="24" CornerRadius="22" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Карточка уведомления" FontSize="18" FontWeight="SemiBold" Foreground="#22303A" />
                            <TextBlock Margin="0,18,0,0" Text="{Binding SelectedTitle}" FontSize="16" FontWeight="SemiBold" Foreground="#0F5C69" TextWrapping="Wrap" />
                            <TextBlock Margin="0,8,0,0" Text="{Binding SelectedMeta}" FontSize="12" Foreground="#687683" TextWrapping="Wrap" />
                            <TextBlock Margin="0,18,0,0" Text="{Binding SelectedMessage}" FontSize="14" LineHeight="23" Foreground="#22303A" TextWrapping="Wrap" />

                            <WrapPanel Margin="0,24,0,0">
                                <Button Margin="0,0,10,10" Command="{Binding MarkSelectedReadCommand}" Content="Отметить прочитанным" Style="{StaticResource MaterialDesignRaisedButton}" />
                                <Button Margin="0,0,10,10" Command="{Binding MarkAllReadCommand}" Content="Прочитать все" Style="{StaticResource MaterialDesignOutlinedButton}" />
                                <Button Margin="0,0,10,10" Command="{Binding RefreshCommand}" Content="Обновить" Style="{StaticResource MaterialDesignOutlinedButton}" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:RoleDashboardSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,22">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.2*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,18,0" Padding="28" CornerRadius="28">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#0F5C69" Offset="0" />
                                <GradientStop Color="#1D7A8A" Offset="0.6" />
                                <GradientStop Color="#6DB9C3" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <TextBlock Text="{Binding HeroTitle}" FontSize="26" FontWeight="Bold" Foreground="White" />
                            <TextBlock Margin="0,12,0,0"
                                       Text="{Binding HeroDescription}"
                                       FontSize="15"
                                       LineHeight="24"
                                       TextWrapping="Wrap"
                                       Foreground="#E4FAFB" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Padding="24" CornerRadius="28" Background="#FFF7E8" BorderBrush="#F5D58C" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding FocusTitle}" FontSize="14" FontWeight="SemiBold" Foreground="#7B5A10" />
                            <TextBlock Margin="0,14,0,0"
                                       Text="{Binding FocusDescription}"
                                       TextWrapping="Wrap"
                                       FontSize="14"
                                       LineHeight="22"
                                       Foreground="#6A5330" />
                        </StackPanel>
                    </Border>
                </Grid>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Metrics}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Grid.Row="2" Padding="26" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Быстрые ориентиры" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                        <ItemsControl Margin="0,24,0,0" ItemsSource="{Binding QuickActions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="330" Margin="0,0,18,18" Padding="18" CornerRadius="20" Background="#F7FAFC" BorderBrush="#E9EEF3" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" Foreground="#22303A" />
                                            <TextBlock Margin="0,8,0,0" Text="{Binding Description}" FontSize="13" TextWrapping="Wrap" Foreground="#667582" />
                                            <TextBlock Margin="0,10,0,0" Text="{Binding Caption}" FontSize="12" Foreground="#0F5C69" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:RoleOrdersSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding Metrics}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.15*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,12,0" Padding="22" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding HeaderTitle}" FontSize="22" FontWeight="Bold" Foreground="#22303A" />
                            <TextBlock Margin="0,10,0,0"
                                       Text="{Binding HeaderSubtitle}"
                                       FontSize="13"
                                       LineHeight="22"
                                       TextWrapping="Wrap"
                                       Foreground="#687683" />

                            <WrapPanel Margin="0,18,0,0">
                                <TextBox Style="{StaticResource CrudTextBoxStyle}"
                                         Width="230"
                                         materialDesign:HintAssist.Hint="Поиск: номер, груз, адрес"
                                         Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}"
                                          Width="180"
                                          materialDesign:HintAssist.Hint="Статус"
                                          ItemsSource="{Binding StatusFilterOptions}"
                                          DisplayMemberPath="Name"
                                          SelectedValuePath="Code"
                                          SelectedValue="{Binding FilterStatusCode, Mode=TwoWay}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Фильтр" Command="{Binding ApplyFilterCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Очистить" Command="{Binding ClearFilterCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Обновить" Command="{Binding RefreshCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="{Binding CreateActionTitle}" Command="{Binding CreateOrderCommand}" Visibility="{Binding IsReceiverMode, Converter={local:BooleanToVisibilityConverter}}" />
                            </WrapPanel>

                            <TextBlock Margin="0,10,0,0"
                                       Text="{Binding StatusMessage}"
                                       TextWrapping="Wrap"
                                       Foreground="#0F5C69" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Padding="22" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding ActionPanelTitle}" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                            <TextBlock Margin="0,10,0,0"
                                       Text="{Binding SelectedOrderSummary}"
                                       FontSize="13"
                                       LineHeight="22"
                                       TextWrapping="Wrap"
                                       Foreground="#687683" />

                            <WrapPanel Margin="0,18,0,0">
                                <StackPanel Margin="0,0,10,10">
                                    <TextBlock Text="Статус" FontSize="12" Foreground="#6A7784" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding SelectedOrderStatusLabel}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                </StackPanel>
                                <StackPanel Margin="0,0,10,10">
                                    <TextBlock Text="{Binding SelectedOrderCounterpartyLabel}" FontSize="12" Foreground="#6A7784" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding SelectedOrderCounterpartyValue}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                </StackPanel>
                                <StackPanel Margin="0,0,10,10">
                                    <TextBlock Text="{Binding SelectedOrderVehicleLabel}" FontSize="12" Foreground="#6A7784" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding SelectedOrderVehicleValue}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                </StackPanel>
                                <StackPanel Margin="0,0,10,10">
                                    <TextBlock Text="Стоимость" FontSize="12" Foreground="#6A7784" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding SelectedOrderCostValue}" FontSize="14" FontWeight="SemiBold" Foreground="#22303A" />
                                </StackPanel>
                            </WrapPanel>

                            <StackPanel Margin="0,6,0,0">
                                <TextBlock Text="{Binding SelectedOrderPickupLabel}" FontSize="12" Foreground="#6A7784" />
                                <TextBlock Margin="0,6,0,0" Text="{Binding SelectedOrderPickupValue}" FontSize="13" TextWrapping="Wrap" Foreground="#22303A" />
                            </StackPanel>

                            <StackPanel Margin="0,12,0,0">
                                <TextBlock Text="{Binding SelectedOrderDeliveryLabel}" FontSize="12" Foreground="#6A7784" />
                                <TextBlock Margin="0,6,0,0" Text="{Binding SelectedOrderDeliveryValue}" FontSize="13" TextWrapping="Wrap" Foreground="#22303A" />
                            </StackPanel>

                            <WrapPanel Margin="0,18,0,0">
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Открыть карточку" Command="{Binding OpenDetailsCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Принять" Command="{Binding AcceptCommand}" Visibility="{Binding IsDriverMode, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Отказаться" Command="{Binding RefuseCommand}" Visibility="{Binding IsDriverMode, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Начать загрузку" Command="{Binding StartLoadingCommand}" Visibility="{Binding IsDriverMode, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Доставлен" Command="{Binding MarkDeliveredCommand}" Visibility="{Binding IsDriverMode, Converter={local:BooleanToVisibilityConverter}}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Подтвердить получение" Command="{Binding ConfirmReceiptCommand}" Visibility="{Binding IsReceiverMode, Converter={local:BooleanToVisibilityConverter}}" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="2"
                        Margin="0,0,0,14"
                        Padding="20"
                        CornerRadius="24"
                        Background="White"
                        BorderBrush="#E8EEF2"
                        BorderThickness="1"
                        Visibility="{Binding IsReceiverMode, Converter={local:BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Margin="0,0,18,0">
                            <TextBlock Text="Мои заявки" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                            <TextBlock Margin="0,8,0,0"
                                       Text="Получатель сначала отправляет заявку, а после обработки у диспетчера по ней создается настоящий заказ."
                                       Foreground="#687683"
                                       TextWrapping="Wrap" />

                            <Border Margin="0,16,0,0"
                                    CornerRadius="18"
                                    BorderBrush="#E8EEF2"
                                    BorderThickness="1"
                                    Visibility="{Binding HasRequests, Converter={local:BooleanToVisibilityConverter}}">
                                <DataGrid Style="{StaticResource SectionDataGridStyle}"
                                          ItemsSource="{Binding Requests}"
                                          SelectedItem="{Binding SelectedRequest, Mode=TwoWay}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="0.6*" />
                                        <DataGridTextColumn Header="Груз" Binding="{Binding Cargo}" Width="1.5*" />
                                        <DataGridTextColumn Header="Маршрут" Binding="{Binding Route}" Width="2.2*" />
                                        <DataGridTextColumn Header="Дата" Binding="{Binding DesiredDate}" Width="1*" />
                                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="1.3*" />
                                        <DataGridTextColumn Header="Заказ" Binding="{Binding CreatedOrderNumber}" Width="1.2*" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <TextBlock Margin="0,14,0,0"
                                       Text="Заявок пока нет."
                                       Foreground="#687683"
                                       Visibility="{Binding HasRequests, Converter={local:InverseBooleanToVisibilityConverter}}" />
                        </StackPanel>

                        <Border Grid.Column="1"
                                Padding="18"
                                CornerRadius="18"
                                Background="#F7FAFC"
                                BorderBrush="#E7EDF2"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Детали заявки" FontSize="16" FontWeight="SemiBold" Foreground="#22303A" />
                                <TextBlock Margin="0,10,0,0" Text="{Binding SelectedRequestSummary}" Foreground="#5F6D79" TextWrapping="Wrap" />
                                <TextBlock Margin="0,12,0,0" Text="{Binding SelectedRequestRoute}" Foreground="#22303A" TextWrapping="Wrap" />
                                <TextBlock Margin="0,10,0,0" Text="{Binding SelectedRequestContacts}" Foreground="#22303A" TextWrapping="Wrap" />
                                <TextBlock Margin="0,10,0,0" Text="{Binding SelectedRequestDate}" Foreground="#22303A" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Row="3" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <DataGrid Style="{StaticResource SectionDataGridStyle}"
                              ItemsSource="{Binding Orders}"
                              SelectedItem="{Binding SelectedOrder, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Номер" Binding="{Binding OrderNumber}" Width="1.1*" />
                            <DataGridTextColumn Header="Груз" Binding="{Binding Cargo}" Width="1.5*" />
                            <DataGridTextColumn Header="Контрагент" Binding="{Binding CounterpartyValue}" Width="1.6*" />
                            <DataGridTextColumn Header="Маршрут" Binding="{Binding Route}" Width="2.2*" />
                            <DataGridTextColumn Header="Транспорт" Binding="{Binding Vehicle}" Width="1.1*" />
                            <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="1.1*" />
                            <DataGridTextColumn Header="Срок" Binding="{Binding DeliveryDate}" Width="1.2*" />
                            <DataGridTextColumn Header="Стоимость" Binding="{Binding Cost}" Width="1.1*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:RoleChecklistSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.8*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,18,0" Padding="26" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Foreground="#22303A" />
                            <TextBlock Margin="0,12,0,0"
                                       Text="{Binding Subtitle}"
                                       FontSize="14"
                                       LineHeight="22"
                                       TextWrapping="Wrap"
                                       Foreground="#687683" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Padding="24" CornerRadius="24" Background="#F5FBFB" BorderBrush="#D5ECEE" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding CalloutTitle}" FontSize="14" FontWeight="SemiBold" Foreground="#0F5C69" />
                            <TextBlock Margin="0,12,0,0"
                                       Text="{Binding CalloutDescription}"
                                       FontSize="13"
                                       LineHeight="22"
                                       TextWrapping="Wrap"
                                       Foreground="#4E7B81" />
                        </StackPanel>
                    </Border>
                </Grid>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,14" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" Foreground="#22303A" />
                                        <TextBlock Margin="0,8,0,0"
                                                   Text="{Binding Description}"
                                                   FontSize="13"
                                                   LineHeight="22"
                                                   TextWrapping="Wrap"
                                                   Foreground="#687683" />
                                    </StackPanel>
                                    <Border Grid.Column="1"
                                            Margin="18,0,0,0"
                                            Padding="12,6"
                                            CornerRadius="14"
                                            Background="#E8F5F7"
                                            VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="SemiBold" Foreground="#0F5C69" />
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AdminDashboardSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,22">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.2*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,18,0" Padding="28" CornerRadius="28">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#0F5C69" Offset="0" />
                                <GradientStop Color="#1D7A8A" Offset="0.6" />
                                <GradientStop Color="#6DB9C3" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel>
                            <TextBlock Text="Центр контроля логистики" FontSize="26" FontWeight="Bold" Foreground="White" />
                            <TextBlock Margin="0,12,0,0" Text="Здесь администратор видит сводку по заказам, пользователям, транспорту и водителям. Блок последних действий вынесен отдельно, как в ТЗ."
                                       FontSize="15" LineHeight="24" TextWrapping="Wrap" Foreground="#E4FAFB" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Padding="24" CornerRadius="28" Background="#FFF7E8" BorderBrush="#F5D58C" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Требует внимания" FontSize="14" FontWeight="SemiBold" Foreground="#7B5A10" />
                            <TextBlock Margin="0,14,0,0" Text="3 заказа просрочены, 1 ТС в ремонте и 2 водителя готовы к новому распределению."
                                       TextWrapping="Wrap" FontSize="14" LineHeight="22" Foreground="#6A5330" />
                        </StackPanel>
                    </Border>
                </Grid>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Metrics}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.55*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,18,0" Padding="26" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="Последние действия пользователей" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                            <ItemsControl Grid.Row="1" Margin="0,24,0,0" ItemsSource="{Binding RecentActivities}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="74" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Width="56" Height="56" CornerRadius="18" Background="#E8F5F7" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Time}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="SemiBold" Foreground="#0F5C69" />
                                            </Border>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" Foreground="#22303A" />
                                                <TextBlock Margin="0,6,0,0" Text="{Binding Description}" FontSize="13" TextWrapping="Wrap" Foreground="#687683" />
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Padding="26" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Быстрые фокусы" FontSize="20" FontWeight="SemiBold" Foreground="#22303A" />
                            <ItemsControl Margin="0,24,0,0" ItemsSource="{Binding QuickActions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,14" Padding="18" CornerRadius="20" Background="#F7FAFC" BorderBrush="#E9EEF3" BorderThickness="1">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" Foreground="#22303A" />
                                                <TextBlock Margin="0,8,0,0" Text="{Binding Description}" FontSize="13" TextWrapping="Wrap" Foreground="#667582" />
                                                <TextBlock Margin="0,10,0,0" Text="{Binding Caption}" FontSize="12" Foreground="#0F5C69" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AdminUsersSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding Stats}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.95*" />
                        <ColumnDefinition Width="1.35*" />
                    </Grid.ColumnDefinitions>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Margin="0,0,12,0" Header="Фильтры" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <WrapPanel>
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Поиск: логин, ФИО, телефон" Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Роль" ItemsSource="{Binding RoleFilterOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding FilterRoleId, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Статус" ItemsSource="{Binding StatusFilterOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding FilterStatusCode, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Сортировка" ItemsSource="{Binding SortOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedSort, Mode=TwoWay}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Фильтр" Command="{Binding ApplyFilterCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Очистить" Command="{Binding ClearFilterCommand}" />
                            </WrapPanel>
                        </Border>
                    </Expander>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Grid.Column="1" Header="{Binding FormTitle}" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                            <WrapPanel>
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Новый" Command="{Binding BeginCreateCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сохранить" Command="{Binding SaveCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Удалить" Command="{Binding DeleteCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сброс" Command="{Binding ResetCommand}" />
                                <TextBlock Margin="8,8,0,0" Text="{Binding StatusMessage}" Foreground="#0F5C69" TextWrapping="Wrap" />
                            </WrapPanel>

                            <WrapPanel>
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Логин" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="{Binding PasswordHint}" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="ФИО / организация" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Роль" ItemsSource="{Binding RoleOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding SelectedRoleId, Mode=TwoWay}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Email *" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Телефон *" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Компания" Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" />
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <CheckBox Margin="0,0,14,0" Content="Активен" IsChecked="{Binding IsActive}" />
                                    <CheckBox Margin="0,0,14,0" Content="Заблокирован" IsChecked="{Binding IsBlocked}" />
                                    <CheckBox Content="Сменить пароль" IsChecked="{Binding MustChangePassword}" />
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </Expander>
                </Grid>

                <Border Grid.Row="2" Margin="0,10,0,0" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <DataGrid Style="{StaticResource SectionDataGridStyle}" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Логин" Binding="{Binding Username}" Width="1.1*" />
                            <DataGridTextColumn Header="ФИО / организация" Binding="{Binding FullName}" Width="2.2*" />
                            <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="1.2*" />
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="1*" />
                            <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="1.2*" />
                            <DataGridTextColumn Header="Последний вход" Binding="{Binding LastLogin}" Width="1.4*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AdminOrdersSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding Stats}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.95*" />
                        <ColumnDefinition Width="1.35*" />
                    </Grid.ColumnDefinitions>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Margin="0,0,12,0" Header="Фильтры" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <WrapPanel>
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Поиск: номер, адрес, груз" Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Статус" ItemsSource="{Binding StatusFilterOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding FilterStatusCode, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Получатель" ItemsSource="{Binding ReceiverFilterOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding FilterReceiverUserId, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Водитель" ItemsSource="{Binding DriverFilterOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding FilterDriverId, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Сортировка" ItemsSource="{Binding SortOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedSort, Mode=TwoWay}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Фильтр" Command="{Binding ApplyFilterCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Очистить" Command="{Binding ClearFilterCommand}" />
                            </WrapPanel>
                        </Border>
                    </Expander>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Grid.Column="1" Header="{Binding FormTitle}" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                            <WrapPanel>
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Создать заказ" Command="{Binding BeginCreateCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Создать по заявке" Command="{Binding CreateFromRequestCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Открыть карточку" Command="{Binding OpenDetailsCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сохранить изменения" Command="{Binding SaveCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Удалить" Command="{Binding DeleteCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сброс" Command="{Binding ResetCommand}" />
                                <TextBlock Margin="8,8,0,0" Text="{Binding StatusMessage}" Foreground="#0F5C69" TextWrapping="Wrap" />
                            </WrapPanel>

                            <Border Margin="0,16,0,0"
                                    Padding="18"
                                    CornerRadius="18"
                                    Background="#F7FAFC"
                                    BorderBrush="#E7EDF2"
                                    BorderThickness="1"
                                    Visibility="{Binding HasPendingRequests, Converter={local:BooleanToVisibilityConverter}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.4*" />
                                        <ColumnDefinition Width="1*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Margin="0,0,18,0">
                                        <TextBlock Text="Входящие заявки"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="#22303A" />
                                        <TextBlock Margin="0,8,0,0"
                                                   Text="Получатель создает короткую заявку, а диспетчер отсюда поднимает по ней полноценный заказ."
                                                   Foreground="#687683"
                                                   TextWrapping="Wrap" />

                                        <Border Margin="0,14,0,0"
                                                CornerRadius="14"
                                                BorderBrush="#E8EEF2"
                                                BorderThickness="1">
                                            <DataGrid Style="{StaticResource SectionDataGridStyle}"
                                                      ItemsSource="{Binding Requests}"
                                                      SelectedItem="{Binding SelectedRequest, Mode=TwoWay}">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="Кто заказывал" Binding="{Binding Receiver}" Width="1.2*" />
                                                    <DataGridTextColumn Header="Какой груз" Binding="{Binding CargoDescription}" Width="1.8*" />
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </Border>
                                    </StackPanel>

                                    <Border Grid.Column="1"
                                            Padding="16"
                                            CornerRadius="16"
                                            Background="White"
                                            BorderBrush="#E8EEF2"
                                            BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="Детали заявки"
                                                       FontSize="15"
                                                       FontWeight="SemiBold"
                                                       Foreground="#22303A" />
                                            <TextBlock Margin="0,10,0,0" Text="{Binding SelectedRequestSummary}" Foreground="#687683" TextWrapping="Wrap" />
                                            <TextBlock Margin="0,10,0,0" Text="{Binding SelectedRequestRoute}" Foreground="#22303A" TextWrapping="Wrap" />
                                            <TextBlock Margin="0,10,0,0" Text="{Binding SelectedRequestContacts}" Foreground="#22303A" TextWrapping="Wrap" />
                                            <TextBlock Margin="0,10,0,0" Text="{Binding SelectedRequestDate}" Foreground="#22303A" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <TextBlock Margin="0,10,0,0" Text="{Binding FormSubtitle}" Foreground="#687683" TextWrapping="Wrap" />

                            <Border Margin="0,16,0,0"
                                    Padding="18"
                                    CornerRadius="18"
                                    Background="#F7FAFC"
                                    BorderBrush="#E7EDF2"
                                    BorderThickness="1"
                                    Visibility="{Binding HasSelectedOrder, Converter={local:InverseBooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="Как теперь создаётся заказ"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#22303A" />
                                    <TextBlock Margin="0,10,0,0"
                                               Text="1. Откройте мастер создания заказа."
                                               Foreground="#5F6D79" />
                                    <TextBlock Margin="0,6,0,0"
                                               Text="2. Укажите получателя и параметры груза по шагам."
                                               Foreground="#5F6D79" />
                                    <TextBlock Margin="0,6,0,0"
                                               Text="3. Заполните маршрут, расчёт и при желании сразу назначьте экипаж."
                                               Foreground="#5F6D79" />
                                </StackPanel>
                            </Border>

                            <StackPanel Margin="0,16,0,0"
                                        Visibility="{Binding HasSelectedOrder, Converter={local:BooleanToVisibilityConverter}}">
                                <WrapPanel>
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Номер заказа" Text="{Binding OrderNumber, UpdateSourceTrigger=PropertyChanged}" />
                                    <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Получатель" ItemsSource="{Binding ReceiverOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding SelectedReceiverUserId, Mode=TwoWay}" />
                                    <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Груз" ItemsSource="{Binding CargoOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding SelectedCargoId, Mode=TwoWay}" />
                                    <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Статус" ItemsSource="{Binding StatusOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedStatusCode, Mode=TwoWay}" />
                                    <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Водитель" ItemsSource="{Binding DriverOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding SelectedDriverId, Mode=TwoWay}" />
                                    <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Транспорт" ItemsSource="{Binding VehicleOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding SelectedVehicleId, Mode=TwoWay}" />
                                    <DatePicker Margin="0,0,10,10" MinWidth="160" materialDesign:HintAssist.Hint="План погрузки" SelectedDate="{Binding PlannedPickupAt, Mode=TwoWay}" DisplayDateStart="{Binding MinSelectableDate, Mode=OneWay}" />
                                    <DatePicker Margin="0,0,10,10" MinWidth="160" materialDesign:HintAssist.Hint="Желаемая доставка" SelectedDate="{Binding DesiredDeliveryAt, Mode=TwoWay}" DisplayDateStart="{Binding MinSelectableDate, Mode=OneWay}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Адрес погрузки" Text="{Binding PickupAddress, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Адрес доставки" Text="{Binding DeliveryAddress, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Км" Text="{Binding DistanceKm, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Стоимость" Text="{Binding TotalCost, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Телефон водителя" Text="{Binding PickupContactPhone, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Телефон получателя" Text="{Binding DeliveryContactPhone, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Причина отмены" Text="{Binding CancellationReason, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Комментарий" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" />
                                </WrapPanel>
                            </StackPanel>
                        </StackPanel>
                        </Border>
                    </Expander>
                </Grid>

                <Border Grid.Row="2" Margin="0,10,0,0" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <DataGrid Style="{StaticResource SectionDataGridStyle}" ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Номер заказа" Binding="{Binding OrderNumber}" Width="1.4*" />
                            <DataGridTextColumn Header="Получатель" Binding="{Binding Receiver}" Width="1.8*" />
                            <DataGridTextColumn Header="Водитель" Binding="{Binding Driver}" Width="1.4*" />
                            <DataGridTextColumn Header="Транспорт" Binding="{Binding Vehicle}" Width="1.2*" />
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="1.1*" />
                            <DataGridTextColumn Header="Плановая доставка" Binding="{Binding DeliveryDate}" Width="1.4*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AdminCargoSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding Stats}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.95*" />
                        <ColumnDefinition Width="1.35*" />
                    </Grid.ColumnDefinitions>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Margin="0,0,12,0" Header="Фильтры" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <WrapPanel>
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Поиск: наименование, тип" Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Тип груза" ItemsSource="{Binding CargoTypeFilterOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding FilterCargoTypeCode, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Сортировка" ItemsSource="{Binding SortOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedSort, Mode=TwoWay}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Фильтр" Command="{Binding ApplyFilterCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Очистить" Command="{Binding ClearFilterCommand}" />
                            </WrapPanel>
                        </Border>
                    </Expander>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Grid.Column="1" Header="{Binding FormTitle}" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                                <WrapPanel>
                                    <Button Style="{StaticResource CrudButtonStyle}" Content="Новый" Command="{Binding BeginCreateCommand}" />
                                    <Button Style="{StaticResource CrudButtonStyle}" Content="Сохранить" Command="{Binding SaveCommand}" />
                                    <Button Style="{StaticResource CrudButtonStyle}" Content="Удалить" Command="{Binding DeleteCommand}" />
                                    <Button Style="{StaticResource CrudButtonStyle}" Content="Сброс" Command="{Binding ResetCommand}" />
                                    <TextBlock Margin="8,8,0,0" Text="{Binding StatusMessage}" Foreground="#0F5C69" TextWrapping="Wrap" />
                                </WrapPanel>

                                <WrapPanel>
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Наименование" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                                    <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Тип груза" ItemsSource="{Binding CargoTypeOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedCargoTypeCode, Mode=TwoWay}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Вес, кг" Text="{Binding WeightKg, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Объём, м3" Text="{Binding VolumeM3, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Описание" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Особые требования" Text="{Binding SpecialRequirements, UpdateSourceTrigger=PropertyChanged}" />
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </Expander>
                </Grid>

                <Border Grid.Row="2" Margin="0,10,0,0" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <DataGrid Style="{StaticResource SectionDataGridStyle}" ItemsSource="{Binding Cargo}" SelectedItem="{Binding SelectedCargo, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="1.8*" />
                            <DataGridTextColumn Header="Тип" Binding="{Binding CargoType}" Width="1.2*" />
                            <DataGridTextColumn Header="Вес" Binding="{Binding Weight}" Width="1*" />
                            <DataGridTextColumn Header="Объём" Binding="{Binding Volume}" Width="1*" />
                            <DataGridTextColumn Header="В заказах" Binding="{Binding UsageCount}" Width="0.8*" />
                            <DataGridTextColumn Header="Обновлён" Binding="{Binding UpdatedAt}" Width="1.2*" />
                            <DataGridTextColumn Header="Особые требования" Binding="{Binding SpecialRequirements}" Width="1.6*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AdminDriversSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding Stats}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.95*" />
                        <ColumnDefinition Width="1.35*" />
                    </Grid.ColumnDefinitions>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Margin="0,0,12,0" Header="Фильтры" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <WrapPanel>
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Поиск: ФИО, ВУ, телефон" Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Статус" ItemsSource="{Binding StatusFilterOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding FilterStatusCode, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Сортировка" ItemsSource="{Binding SortOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedSort, Mode=TwoWay}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Фильтр" Command="{Binding ApplyFilterCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Очистить" Command="{Binding ClearFilterCommand}" />
                            </WrapPanel>
                        </Border>
                    </Expander>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Grid.Column="1" Header="{Binding FormTitle}" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                            <WrapPanel>
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Новый" Command="{Binding BeginCreateCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сохранить" Command="{Binding SaveCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Удалить" Command="{Binding DeleteCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сброс" Command="{Binding ResetCommand}" />
                                <TextBlock Margin="8,8,0,0" Text="{Binding StatusMessage}" Foreground="#0F5C69" TextWrapping="Wrap" />
                            </WrapPanel>

                            <WrapPanel>
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Пользователь" ItemsSource="{Binding DriverUserOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding SelectedDriverUserId, Mode=TwoWay}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Номер ВУ" Text="{Binding LicenseNumber, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Категория" Text="{Binding LicenseCategory, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Стаж" Text="{Binding ExperienceYears, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Статус" ItemsSource="{Binding StatusOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedStatusCode, Mode=TwoWay}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Заметки" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </Expander>
                </Grid>

                <Border Grid.Row="2" Margin="0,10,0,0" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <DataGrid Style="{StaticResource SectionDataGridStyle}" ItemsSource="{Binding Drivers}" SelectedItem="{Binding SelectedDriver, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="2*" />
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="1*" />
                            <DataGridTextColumn Header="Номер ВУ" Binding="{Binding LicenseNumber}" Width="1.4*" />
                            <DataGridTextColumn Header="Стаж" Binding="{Binding Experience}" Width="1*" />
                            <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="1.2*" />
                            <DataGridTextColumn Header="Текущий заказ" Binding="{Binding CurrentOrder}" Width="1.3*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AdminVehiclesSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding Stats}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.95*" />
                        <ColumnDefinition Width="1.35*" />
                    </Grid.ColumnDefinitions>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Margin="0,0,12,0" Header="Фильтры" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <WrapPanel>
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Поиск: госномер, модель, водитель" Text="{Binding FilterSearch, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Статус" ItemsSource="{Binding StatusFilterOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding FilterStatusCode, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Тип кузова" ItemsSource="{Binding BodyTypeFilterOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding FilterBodyTypeCode, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Сортировка" ItemsSource="{Binding SortOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedSort, Mode=TwoWay}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Фильтр" Command="{Binding ApplyFilterCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Очистить" Command="{Binding ClearFilterCommand}" />
                            </WrapPanel>
                        </Border>
                    </Expander>

                    <Expander Style="{StaticResource SectionExpanderStyle}" Grid.Column="1" Header="{Binding FormTitle}" IsExpanded="False">
                        <Border Padding="18" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                            <StackPanel>
                            <WrapPanel>
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Новый" Command="{Binding BeginCreateCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сохранить" Command="{Binding SaveCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Удалить" Command="{Binding DeleteCommand}" />
                                <Button Style="{StaticResource CrudButtonStyle}" Content="Сброс" Command="{Binding ResetCommand}" />
                                <TextBlock Margin="8,8,0,0" Text="{Binding StatusMessage}" Foreground="#0F5C69" TextWrapping="Wrap" />
                            </WrapPanel>

                            <WrapPanel>
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Госномер" Text="{Binding LicensePlate, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Модель" Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Грузоподъемность, кг" Text="{Binding CapacityKg, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Объем, м3" Text="{Binding VolumeM3, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Тип кузова" ItemsSource="{Binding BodyTypeOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedBodyTypeCode, Mode=TwoWay}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Год выпуска" Text="{Binding ProductionYear, UpdateSourceTrigger=PropertyChanged}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Статус" ItemsSource="{Binding StatusOptions}" DisplayMemberPath="Name" SelectedValuePath="Code" SelectedValue="{Binding SelectedStatusCode, Mode=TwoWay}" />
                                <ComboBox Style="{StaticResource CrudComboBoxStyle}" materialDesign:HintAssist.Hint="Закрепленный водитель" ItemsSource="{Binding DriverOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding SelectedCurrentDriverId, Mode=TwoWay}" />
                                <DatePicker Margin="0,0,10,10" MinWidth="160" materialDesign:HintAssist.Hint="Страховка до" SelectedDate="{Binding InsuranceExpiry, Mode=TwoWay}" />
                                <TextBox Style="{StaticResource CrudTextBoxStyle}" materialDesign:HintAssist.Hint="Заметки" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </Expander>
                </Grid>

                <Border Grid.Row="2" Margin="0,10,0,0" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                    <DataGrid Style="{StaticResource SectionDataGridStyle}" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle, Mode=TwoWay}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Госномер" Binding="{Binding LicensePlate}" Width="1.1*" />
                            <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="1.7*" />
                            <DataGridTextColumn Header="Тип кузова" Binding="{Binding BodyType}" Width="1.2*" />
                            <DataGridTextColumn Header="Грузоподъемность" Binding="{Binding Capacity}" Width="1.2*" />
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="1.1*" />
                            <DataGridTextColumn Header="Закрепленный водитель" Binding="{Binding AssignedDriver}" Width="1.5*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:AdminReportsSectionViewModel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding Stats}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,16,16" Padding="20" CornerRadius="20" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#6A7784" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Foreground="#1E2B35" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding Hint}" FontSize="13" TextWrapping="Wrap" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl Grid.Row="1" Margin="0,18,0,0" ItemsSource="{Binding Reports}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="310" Margin="0,0,18,18" Padding="24" CornerRadius="24" Background="White" BorderBrush="#E8EEF2" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="#22303A" />
                                    <TextBlock Margin="0,10,0,0" Text="{Binding Description}" TextWrapping="Wrap" FontSize="13" LineHeight="22" Foreground="#687683" />
                                    <Border Margin="0,18,0,0" Padding="10,6" HorizontalAlignment="Left" CornerRadius="14" Background="#E8F5F7">
                                        <TextBlock Text="{Binding Format}" FontSize="12" FontWeight="SemiBold" Foreground="#0F5C69" />
                                    </Border>
                                    <TextBlock Margin="0,14,0,0" Text="{Binding Freshness}" FontSize="12" Foreground="#6A7784" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid Background="#EEF3F7">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Margin="18"
                CornerRadius="32">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0E4F59" Offset="0" />
                    <GradientStop Color="#176C7A" Offset="0.55" />
                    <GradientStop Color="#4D98A2" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="28,28,28,18">
                    <TextBlock Text="Грузоперевозки"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="White" />
                    <TextBlock Margin="0,10,0,0"
                               Text="{Binding ShellSubtitle}"
                               FontSize="14"
                               LineHeight="22"
                               TextWrapping="Wrap"
                               Foreground="#D8F1F2" />
                </StackPanel>

                <Border Grid.Row="1"
                        Margin="28,0,28,22"
                        Padding="18"
                        CornerRadius="22"
                        Background="#18000000">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentUserName}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="White" />
                        <TextBlock Margin="0,4,0,0"
                                   Text="{Binding CurrentUserRole}"
                                   FontSize="13"
                                   Foreground="#D8F1F2" />
                        <TextBlock Margin="0,12,0,0"
                                   Text="{Binding SessionLabel}"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Foreground="#C6E8EA" />
                    </StackPanel>
                </Border>

                <ListBox Grid.Row="2"
                         Margin="20,0,20,0"
                         ItemsSource="{Binding NavigationItems}"
                         SelectedItem="{Binding SelectedNavigationItem}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,0,0,10" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Foreground" Value="#D7F1F2" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="NavigationItemBorder"
                                                Padding="18,16"
                                                CornerRadius="20"
                                                Background="#13000000">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="NavigationItemBorder" Property="Background" Value="#F4C95D" />
                                                <Setter Property="Foreground" Value="#17313A" />
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="NavigationItemBorder" Property="Background" Value="#24FFFFFF" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}" />
                                    <TextBlock Margin="0,6,0,0"
                                               Text="{Binding Subtitle}"
                                               FontSize="12"
                                               TextWrapping="Wrap"
                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}" />
                                </StackPanel>
                                <Border Grid.Column="1"
                                        Padding="10,4"
                                        CornerRadius="12"
                                        Background="#1AFFFFFF"
                                        VerticalAlignment="Top">
                                    <TextBlock Text="{Binding Badge}"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Row="3"
                        Margin="28,18,28,28"
                        Padding="18"
                        CornerRadius="22"
                        Background="#18000000">
                    <StackPanel>
                        <TextBlock Text="Подключение"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="White" />
                        <TextBlock Margin="0,10,0,0"
                                   Text="MariaDB / локальный сервер"
                                   FontSize="13"
                                   Foreground="#D8F1F2" />
                        <TextBlock Margin="0,6,0,0"
                                   Text="{Binding CurrentUserLogin}"
                                   FontSize="12"
                                   Foreground="#C6E8EA" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Margin="0,18,18,18"
                Padding="26"
                CornerRadius="30"
                Background="White"
                BorderBrush="#E5ECF1"
                BorderThickness="1"
                materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding SelectedNavigationTitle}"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#22303A" />
                        <TextBlock Margin="0,8,0,0"
                                   Text="{Binding SelectedNavigationSubtitle}"
                                   FontSize="14"
                                   TextWrapping="Wrap"
                                   Foreground="#6B7985" />
                    </StackPanel>

                    <Border Grid.Column="1"
                            Margin="18,0,0,0"
                            Padding="16,10"
                            CornerRadius="18"
                            Background="#F6FAFC"
                            VerticalAlignment="Center">
                        <StackPanel>
                            <TextBlock Text="Сегодня"
                                       FontSize="11"
                                       Foreground="#6A7784" />
                            <TextBlock Margin="0,2,0,0"
                                       Text="{Binding TodayLabel}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#22303A" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2"
                            Margin="14,0,0,0"
                            Padding="18,10"
                            CornerRadius="18"
                            Background="#E8F5F7"
                            VerticalAlignment="Center">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding CurrentUserName}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="#0F5C69" />
                                <TextBlock Margin="0,2,0,0"
                                           Text="{Binding CurrentUserRole}"
                                           FontSize="12"
                                           Foreground="#4E7B81" />
                            </StackPanel>

                            <Button Grid.Column="1"
                                    Margin="16,0,0,0"
                                    MinWidth="82"
                                    Height="34"
                                    Padding="14,0"
                                    Command="{Binding LogoutCommand}"
                                    Content="Выйти"
                                    Style="{StaticResource MaterialDesignOutlinedButton}" />
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="1">
                    <!-- Background Content -->
                    <Border Margin="0,26,0,0"
                            Padding="24"
                            CornerRadius="26"
                            Background="#F8FBFD">
                        <ContentPresenter Content="{Binding SelectedContent}" />
                    </Border>

                    <!-- Modal Overlay -->
                    <Grid Visibility="{Binding ActiveModalContent, Converter={local:NullToVisibilityConverter}}">
                        <Border Background="#40000000" />
                        <Border Background="White"
                                CornerRadius="24"
                                Width="1040"
                                Height="720"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                BorderBrush="#E5ECF1"
                                BorderThickness="1">
                            <Grid>
                                <Button Width="34"
                                        Height="34"
                                        Margin="0,12,12,0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Panel.ZIndex="2"
                                        Command="{Binding CloseModalCommand}"
                                        Content="×"
                                        FontSize="20"
                                        FontWeight="SemiBold"
                                        Style="{StaticResource MaterialDesignOutlinedButton}" />
                                <ContentPresenter Margin="0,42,0,0"
                                                  Content="{Binding ActiveModalContent}" />
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
